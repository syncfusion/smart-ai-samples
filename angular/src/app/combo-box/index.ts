import { LocalEmbeddingComponent } from './local-embedding/local-embedding.component';

import {     Routes } from '@angular/router';
export const comboboxRoutes:Routes  = [{
    path: 'combobox/local-embedding',component: LocalEmbeddingComponent
},
];

